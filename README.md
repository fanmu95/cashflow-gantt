# 💰 资金计划甘特图（现金流日历）

一个纯前端的个人资金计划工具：在日历上填写预计入账 / 花费，自动计算每日累计剩余，负余额日期整格标红预警。单文件、零依赖、离线可用。

## 功能特性

- **日历甘特视图**：按周排列的月历，每格显示当日入账(+绿) / 花费(-红) / 净额 / 累计剩余
- **点击填数**：点击任意日期，添加、编辑（含调整日期）、删除当天的收支项目
- **负余额预警**：累计剩余为负的日期整格红底 + ⚠ 标记，并在详情弹窗给出提示
- **悬停预览**：鼠标悬停日期即可快速预览当天项目清单与累计剩余
- **明细表**：底部汇总全部项目（按日期排序），支持类型 / 月份筛选，合计行自动更新，可直接编辑 / 删除
- **周期账单**：设置每月 / 每周 / 每两周规则，浏览到对应月份自动生成（如房贷、车贷）
- **回收站**：删除的项目保留 30 天，可恢复或彻底删除
- **Excel 导出**：一键把当前筛选结果导出为 `.xlsx`（纯本地生成，无需联网）
- **深浅色主题**：一键切换，选择会记住
- **云同步（可选）**：数据可上传到你的 GitHub Secret Gist，支持**自动同步**（定时上传 / 拉取、冲突保护）

## 快速开始

直接用浏览器打开 `index.html` 即可，无需安装任何依赖。

> 也可在线访问（GitHub Pages）：<https://fanmu95.github.io/cashflow-gantt/>

## 云同步使用说明

1. 打开页面右上角 ☁️，填入自己的 GitHub Token（需勾选 `gist` 权限，[点此创建](https://github.com/settings/tokens/new)）
2. 点「⬆️ 上传到云端」：首次会自动创建一个 **Secret Gist** 存放数据（他人搜索不到）
3. 勾选「自动同步」并选好间隔：之后每次打开页面都会用缓存的 Token 自动上传 / 拉取
4. 换设备时：打开同一页面，填入同一 Token，点「⬇️ 从云端下载」即可继续

> **页面本身不携带任何密钥或个人数据**：Token 只缓存在你自己的浏览器（localStorage），不会写进页面、数据文件或仓库。
> 本地和云端都有新改动时，自动同步会**暂停并提示**，由你手动选择「上传」或「下载」，不会静默覆盖。
> 如果你把本页面分享给别人，对方看到的是全新空白账本——他需要填入自己的 Token 才能使用云同步。

## 数据存储与隐私

- 所有数据保存在**浏览器本地（localStorage）**，不上传任何服务器
- 「导出 / 导入」可备份为 JSON 文件；自动同步拉取覆盖前也会在本地留一份回滚副本
- 云同步功能需要你自己提供 GitHub Token，数据只存于你的私密 Gist；Token 仅保存在本机浏览器，不会写入数据文件
- 页面不含任何个人数据，账单数据完全由使用者自行录入

## 技术说明

- 单文件 HTML（HTML + CSS + 原生 JavaScript），无任何外部依赖、无构建步骤
- 图表为原生 SVG 绘制，Excel 导出为纯 JS 实现的 ZIP + OOXML 生成器，均离线可用

## 许可

个人使用项目。
